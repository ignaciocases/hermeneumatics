package example

object Example {
	def main(args: Array[String]): Unit = {}

	def helloWorld(s: String): String = {
		val myList = List(1,2,3)
		"Hello World!" + myList.map(e => e + 1).mkString("[","-","]") + s
	}
}
